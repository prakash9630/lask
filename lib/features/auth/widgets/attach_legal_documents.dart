import 'package:flutter/material.dart';

class AttachLegalDocuments extends StatelessWidget {
  const AttachLegalDocuments({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Attach Legal Documents",
      style: TextStyle(
        fontWeight: FontWeight.w600,
        fontSize: 16,
      ),
    );
  }
}
